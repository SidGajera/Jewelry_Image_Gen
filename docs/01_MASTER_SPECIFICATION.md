# 01 — MASTER SPECIFICATION

**Project:** Lucent Carat Lab — AI Catalog Image Generation
**Operator account:** `lucentcaratlab@gmail.com` (NEVER `houseoflunivae@gmail.com`)
**Canonical merged spec:** `/LUCENT_MASTER.md` (root). This file is the architectural overview; `LUCENT_MASTER.md` holds the full rule text. Both must agree — if they diverge, `LUCENT_MASTER.md` §-numbers win and this file is updated to match.

---

## 1. PURPOSE
For each jewelry SKU, take the brand's SOURCE product images and generate **12 marketing photos** that keep the jewelry **100 % identical** to the source, on the brand's **locked white studio cloth with the printed brand logo**. Output: photorealistic, **1:1 aspect, 2K resolution**, images only.

## 2. THE 12 IMAGES PER CATALOG
> **SUPERSEDED for new catalogs (user-locked 2026-07-21):** the current default is **10 images = 7 closed-hand (worn) + 3 velvet-background studio** — see `docs/10 → DEFAULT CATALOG COMPOSITION`. The 12-image breakdown below is retained as the historical/base structure and for catalogs that explicitly request it.

| Group | Count | What |
|---|---|---|
| STUDIO | 5 | Ring on locked white cloth + printed logo. 5 different angles: front top-down, macro close-up, side profile, held-on-cloth-fold, three-quarter rest. |
| LIFESTYLE (wider) | 4 | Ring worn on a natural hand in a cozy warm US-home. NO desks/laptops/offices. Each a different pose. |
| CLOSE-UP | 3 | Macro of the ring (worn or held) in the same cozy-home theme. Each a different pose. |

- Each image generated ONCE (`count=1`).
- **ALL images 1:1 and 2K.** No exceptions.
- Poses vary every catalog (different pose references each SKU) so it looks like a real photoshoot.

## 3. ARCHITECTURE
```
Google Drive (source SKUs, reference poses, logo asset, output)
        │  (READ / SEARCH / CREATE only — connector cannot edit/delete)
        ▼
Study source ring (download_file_content → decode → view)  [0 credits]
        │
        ▼
media_import_url (Drive uc?id=…&export=download → Higgsfield media_id)  [server-side fetch]
        │
        ▼
Higgsfield generate_image  (the production model, resolution 2k, 1:1)   [2 credits/img]
   medias = [ pose/studio reference , SOURCE ring ]
        │   (jewelry generated by AI on the locked clean cloth — NO legible logo rendered)
        ▼
User reviews outputs in the Higgsfield app
        │
        ▼
LOCAL post-processing  (Python/PIL, 0 credits, run by user on downloaded outputs):
   • whiten_cloth.py            → force pure neutral white cloth
   • scripts/print_logo_on_cloth.py → composite the LOCKED logo so it looks printed on the cloth
        │
        ▼
Final catalog images → Drive Output folder + tracking log
```

## 4. GENERATION PIPELINE (per SKU, lean)
1. **1 Drive search** for the SKU source folder → **1 search** for its images.
2. **Study the ring** (download top view + 1 side view, decode to PNG, view) — verify design; ignore stray/mislabeled files.
3. **Import** the source ring once (`media_import_url`). Reuse cached studio branded refs; import fresh lifestyle/closeup pose refs (different poses than last SKU).
4. **Fire all 12** `generate_image` calls in ONE batch, compact full-quality prompts, `resolution:"2k"`, `aspect_ratio:"1:1"`.
5. **Report count only** ("LR-XXXX done: 5 studio + 4 lifestyle + 3 closeup"). No previews.
6. **Local post:** whiten cloth if warm; composite the printed logo on studio shots.
7. **Log** the SKU in `LUCENT_MASTER.md` progress + product codes; commit docs to the repo.

## 5. QUALITY STANDARDS (summary — full text in docs 03)
- Jewelry 100 % identical to source (design, stones, count, setting, prongs, band, metal). Change nothing.
- Diamonds: single real brilliant-cut facet pattern, natural bright+dark facet mix, crystal clear, NO doubling/CGI. Small pavé as crisp as the center. (Benchmark: LR-0156 studio shot.)
- Metal: 18K natural warm yellow gold (unless the source is otherwise).
- Cloth: locked material + pure neutral white, consistent every image.
- Logo: printed-on-cloth look, pixel-identical to the locked asset, never AI-drawn.
- Light/sparkle: natural, realistic; no over-lighting, CGI glow, starburst, rainbow fire.
- Ring tack-sharp in every shot; only background blurs.

## 6. SUCCESS CRITERIA (an image PASSES only if ALL are true)
1. Ring is indistinguishable in design from the source (stones, count, cut, setting, prongs, band, metal).
2. Center + accent stones render as real diamonds/gems (no doubled facets, no glassy dots, no recolor — e.g. green emeralds stay green).
3. Studio shots: locked white cloth, neutral white, printed logo pixel-identical to `assets/logo/logo_official.png` (no "ELLYREID"-type hallucination).
4. Lifestyle/closeups: cozy warm US-home, natural five-finger hand, no laptop/desk, no invented logo.
5. 1:1, 2K, tack-sharp ring.
6. Consistency: looks like the same cloth/studio/lighting/camera/white-balance as every other catalog image.
Any failure → the generation is FAILED and must be redone/fixed (logo issues are fixed locally, 0 credits).
