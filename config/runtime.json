{
  "purpose": "SINGLE startup file. On a new session read ONLY this file, then reply 'READY — 0 ACTIVE PROCESSES'. Do not scan/preload the repo; load another file only when a task needs it.",
  "clean_session_start": {
    "cancel_previous": "cancel/ignore all prior image-gen jobs, retries, polling/verify loops, downloads, uploads, background tasks",
    "do_not_resume": true,
    "do_not_poll_old_jobs": true,
    "do_not_restore": "old history, temp data, base64, media previews, logs, tool outputs",
    "clear_runtime_state_only": "never delete approved assets, rules, source images, SKU records, approved catalog results",
    "active_processes_at_start": 0
  },
  "token_safe_execution": {
    "no_action_until_task": "do not generate/verify/poll/commit/call tools until the user submits a task",
    "never_reload_unchanged_files": true,
    "never_reopen_approved_images": true,
    "never_repeat_established_rules": true,
    "max_response_lines": 10,
    "one_tool_call_when_possible": true,
    "no_parallel_jobs_unless_requested": true,
    "no_silent_retries": true,
    "stop_when_task_done": true,
    "output_must_not_change": "OPTIMIZE TOKENS ONLY. Output stays visually+functionally identical: jewelry geometry/proportions, diamond shape/facets/brilliance, prong count/setting/basket/gallery/band, logo accuracy+cloth integration, bright neutral-white light, camera-angle quality+per-catalog variety, physics/shadows/product focus, Higgsfield quality (2k/model/reference strength), Drive+verification depth, approval+direct-to-main workflow. Never drop a QC step or lower resolution/model/reference/verification. Priority: 1 exact output, 2 exact jewelry identity, 3 full verification, 4 tokens. If a saving could change output or reliability, do not use it.",
    "cache_drive_ids_after_first_lookup": true,
    "read_each_source_image_once_per_sku": true,
    "reuse_approved_design_profile": true,
    "never_relist_unchanged_folders": true,
    "never_echo_full_prompts_urls_schemas_metadata_or_tool_results_in_chat": true,
    "avoid_polling_and_duplicate_tool_calls": true,
    "batch_equivalent_operations_when_output_identical": true,
    "never_reload_unchanged_tool_schemas": true,
    "github_only_after_catalog_approval": true,
    "token_safety_policy": "Prevent token/context exhaustion at all costs. Continuously watch the remaining budget; when it is critically low (before the session risks stopping) do NOT start any new operation - stop launching image generations, edits, searches, file scans, polling, commits, or background tasks, and cancel/avoid any non-essential not-yet-started work. Finish only the current atomic operation if already running and safe to complete; otherwise stop immediately. Never begin another catalog/image/workflow when the budget is low. Instead return a concise status summary: completed work, pending work, current checkpoint, and files/commits/outputs already saved."
  },
  "commit_policy": {
    "during_generation": "no commits",
    "during_corrections": "no commits",
    "on_explicit_catalog_approval": "exactly ONE commit of approved catalog files + compact approval record",
    "commit_message": "Approve catalog <SKU>",
    "no_readme_changelog_version_doc_updates_unless_requested": true
  },
  "output_lock": "token optimization must never alter jewelry geometry, diamonds, prongs, settings, lighting, logo, cloth, physics, or image quality",
  "pointers": {
    "rules_entry": "CLAUDE_SETUP.md",
    "runtime_trio": ["config/project_manifest.json", "prompts/07_PROMPTS.md", "config/QUALITY_MEMORY.json"],
    "verify_inbox_drive_id": "1idwSM7EvXY7MijTZr-5FQ5sZmCnIQaX0",
    "repository": "https://github.com/SidGajera/Claude_Lucent_Image_Gen"
  },
  "load_rule": "Load a file only when runtime.json or the current task explicitly requires it. Reuse asset paths/checksums without opening assets until needed."
}
